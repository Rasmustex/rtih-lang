# This program prints the fibonacci sequence up until and including 6765
# It also demonstrates how goto could be used for the creation of routines

goto fib
ret:

# Imagine some code here that depends on the side effects of fib on the stack

0 exit

fib:
0 dup . 1 dup .
loop:
dup2
+
dup
.
dup
6765 = if
goto ret
end
goto loop 